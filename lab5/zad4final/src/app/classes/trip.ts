export class Trip {
    key: string
    name: string
    destination: string
    beginDate: string
    endDate: string
    price: number
    maxCapacity: number
    description: string
    imgURL: string
    raiting = 0
    places = 0
}
